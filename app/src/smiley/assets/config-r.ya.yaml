libCommons:
  RemoteConfig:
    PlistFile: "Local.ic"
    PlistUrl: "http://cdn.emoji.mobi/androidemojikeyboard/configs/config-emojikeyboard-2.1.3.ic"
  Analytics:
      FlurryKey: "FC2GWXDVFTZHFK7KX6KV"
      FacebookID: "1474873346171463"
      FlyerKey: "d4VKN2yBPk7gqTRBZcwkbc"
      GoogleTrackId: ""
      FlurryStoredMaxCount: 1000
      GooglePercentage: 1000
  DiverseSession:
    AppManageDiverseSession: true
  ImageCache:
    CacheSize: 0

  Market:
    DefaultMarket: "Google"
    3rdChannel: "GP"
    Markets:
      Google:
        AppPackage: "com.android.vending"
        AppUrl: "market://details?id="
        WebUrl: "https://play.google.com/store/apps/details?id="
      Amazon:
        AppPackage: "com.amazon.venezia"
        AppUrl: "amzn://apps/android?p="
        WebUrl: "http://www.amazon.com/gp/mas/dl/android?p="

  Connection:
      SigKey: "610_1"
      HashKey: "f4ilz;_aePCR/qYYl05<Wgu)O[Y{ZQxo"
  AppId: "610"
libFramework:
  Push:
    SenderID: "805407701758"
    SendTokenToServer: yes
  Alerts:
    LocalFile: "Alerts.ic"
    RemoteUrl: "http://cdn.emoji.mobi/androidemojikeyboard/alerts/emojikeyboard-2.1.3-alerts.ic"

libKeyboard:
  DictionaryUrl: "http://cdn.emoji.mobi/googlekeyboard/4.0/"

Application:
  WallpaperAPIURL: "https://service.appcloudbox.net/wallpapers/"

adAdapter:
  admobNative:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  admobBanner:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  admobInterstitial:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  adxNative:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  adxBanner:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  adxInterstitial:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  openxBanner:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化
  ironsourceInterstitial:
    initsdkwhenstart : false     //false表示在 load广告时进行SDK的初始化，true表示在App启动时进行初始化，如果没有配置默认是在App启动时进行SDK初始化