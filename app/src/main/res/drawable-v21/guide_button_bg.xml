<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/guide_bg_ripple_color">
    <!-- Ripple mask - applied to all selector states -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/guide_bg_radius" />
            <stroke
                android:width="@dimen/theme_detail_bg_stroke_width"
                android:color="@color/black_standard" />
            <solid android:color="@color/white_standard" />
        </shape>
    </item>
    <item>
        <selector>
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <corners android:radius="@dimen/guide_bg_radius" />
                    <solid android:color="@color/guide_bg_disable_color" />
                </shape>
            </item>

            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="@dimen/guide_bg_radius" />
                    <solid android:color="@color/guide_bg_normal_color" />
                </shape>
            </item>

        </selector>
    </item>
</ripple>